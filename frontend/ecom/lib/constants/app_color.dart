import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const Color scaffoldBackgroundColor = Colors.white;
  static const Color primary = Colors.black;
  static const Color secondary = Color.fromARGB(255, 66, 66, 66);
  // Accent and secondary

  static const Color backgroundColorAppBar = Colors.white;
  static const Color foregroundColorAppbar = Colors.black;
  static const Color scaffold = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);

  // Border and divider
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFBDBDBD);

  // Error and success
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFFBC02D);

  // Disabled & input
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color inputFill = Color(0xFFF1F1F1);
}
